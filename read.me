install dart like decribed on https://dart.dev/get-dart
(windows needs chocolatey: https://chocolatey.org/install)

install local lib (do "drat pub get" in base dir of this repo)

get or create the secrets.darts file (define string password and username, like used in the nissan connect app)

run by "dart ./example/query_leaf.dart"
compile by "dart compile exe ./example/query_leaf.dart"

./example/test.py is an example to run the query as a independed task from python


